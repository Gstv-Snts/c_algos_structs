#ifndef ARRAY_H
#define ARRAY_H
/*
 * Find the index of the array that has the target value.
 * @param1 The array to be searched.
 * @param2 The value to be found.
 * @return The index of target number if found, -1 if not found.
 */
int findArrayValueIndex(int arr[], int value);
#endif
